/**
 * ==========================================
 * ลำดับการทำงาน: [ลำดับที่ 39 / Flow ขั้นตอนที่ 39]
 * ชื่อไฟล์: page.tsx
 * หน้าที่หลัก: Route Page ลืมรหัสผ่าน (`/forgot-password`)
 * รับอะไรมาจากไหน (Input): HTTP GET Request
 * ส่งอะไรไปที่ไหนต่อ (Output / Target): Render ฟอร์มกรอกอีเมลรีเซ็ตรหัสผ่าน
 * ==========================================
 */

import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Forgot Password',
};

export default function ForgotPasswordPage() {
  return <div>ForgotPasswordPage</div>;
}
